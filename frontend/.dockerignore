# Never bake local env or build output into the frontend image.
# NEXT_PUBLIC_* values arrive via Docker build args, not env files.
.env*
node_modules/
.next/
*.session
test-results/
playwright-report/
e2e/.auth/
