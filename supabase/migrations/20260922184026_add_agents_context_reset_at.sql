-- Сброс контекста агента (issue #70).
-- Краткосрочный контекст хода собирается из agent_messages за последние часы;
-- сообщения раньше этой отметки в него больше не попадают. Сама история
-- остаётся в базе и видна на дашборде — сбрасывается только то, что видит модель.
alter table public.agents
    add column context_reset_at timestamptz;
